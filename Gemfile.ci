#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'locomotivecms-solid', '~> 0.2.2.1'
end

group :development do
  gem 'rake', '~> 10.3.2'
end

group :development, :test do
  gem 'rspec', '~> 3.1.0'
end